#!/bin/bash
#==================================================================================================
#
#  Run script for the smatdmatsub module of the Blaze test suite
#
#  Copyright (C) 2011 Klaus Iglberger - All Rights Reserved
#
#  This file is part of the Blaze library. This library is free software; you can redistribute
#  it and/or modify it under the terms of the GNU General Public License as published by the
#  Free Software Foundation; either version 3, or (at your option) any later version.
#
#  This library is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY;
#  without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
#  See the GNU General Public License for more details.
#
#  You should have received a copy of the GNU General Public License along with a special
#  exception for linking and compiling against the Blaze library, the so-called "runtime
#  exception"; see the file COPYING. If not, see http://www.gnu.org/licenses/.
#
#==================================================================================================


PATH_SMATDMATSUB=$( dirname "${BASH_SOURCE[0]}" )

echo " Running sparse matrix/dense matrix subtraction tests...";

$PATH_SMATDMATSUB/MCaM2x2a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM2x2b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM3x3a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM3x3b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM4x4a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM4x4b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM5x5a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM5x5b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM6x6a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM6x6b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM7x13a; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM7x13b; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM16x8a; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaM16x8b; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaMDa;    if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCaMDb;    if [ $? != 0 ]; then exit 1; fi

$PATH_SMATDMATSUB/MCbM2x2a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM2x2b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM3x3a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM3x3b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM4x4a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM4x4b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM5x5a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM5x5b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM6x6a;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM6x6b;  if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM7x13a; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM7x13b; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM16x8a; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbM16x8b; if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbMDa;    if [ $? != 0 ]; then exit 1; fi
$PATH_SMATDMATSUB/MCbMDb;    if [ $? != 0 ]; then exit 1; fi

$PATH_SMATDMATSUB/AliasingTest; if [ $? != 0 ]; then exit 1; fi
